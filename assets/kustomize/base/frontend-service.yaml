apiVersion: v1
kind: Service
metadata:
  name: multi-agent-frontend
  labels:
    app: multi-agent-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: multi-agent-frontend
    component: frontend
