# HTTPRoute for multi-agent frontend
# Routes traffic from Istio Gateway to the frontend service
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: multi-agent-frontend-route
spec:
  parentRefs:
  - name: multi-agent-gateway
    namespace: istio-ingress-dev
  hostnames:
  - "frontend.bytecourier.local"
  rules:
  # WebSocket route with disabled timeout for long-lived connections
  - matches:
    - path:
        type: PathPrefix
        value: /api/ws/
    backendRefs:
    - name: multi-agent-frontend
      port: 3000
    timeouts:
      request: 0s
      backendRequest: 0s
  # Regular HTTP route
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: multi-agent-frontend
      port: 3000
