apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata:
  name: backend-jwt-auth
spec:
  targetRefs:
    - kind: Gateway
      group: gateway.networking.k8s.io
      name: backend-waypoint
  jwtRules:
    - issuer: "spark-chat"
      jwksUri: "http://auth.rag-agent-dev.svc.cluster.local:8000/.well-known/jwks.json"
      fromHeaders:
        - name: Authorization
          prefix: "Bearer "
      fromParams:
        - token
      forwardOriginalToken: true
