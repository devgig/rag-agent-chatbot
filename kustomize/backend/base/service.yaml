apiVersion: v1
kind: Service
metadata:
  name: rag-agent-backend
  labels:
    app: rag-agent-backend
spec:
  selector:
    app: rag-agent-backend
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
