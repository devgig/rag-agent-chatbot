apiVersion: v1
kind: Service
metadata:
  name: multi-agent-backend
  labels:
    app: multi-agent-backend
spec:
  selector:
    app: multi-agent-backend
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: LoadBalancer
