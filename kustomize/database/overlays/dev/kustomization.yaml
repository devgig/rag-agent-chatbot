apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: multi-agent-database-dev

namespace: multi-agent-dev

resources:
  - ../../base

labels:
- includeSelectors: false
  pairs:
    environment: dev
