apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: embedding

resources:
  - embedding-deployment.yaml
  - embedding-service.yaml

patches:
  - target:
      kind: Deployment
      name: embedding
    path: embedding-image-patch.yaml
