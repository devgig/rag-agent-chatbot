apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: qwen25-vl-7b-dev

namespace: rag-agent

resources:
  - namespace.yaml
  - ../../base

labels:
- includeSelectors: false
  pairs:
    environment: dev

# Dev-specific configuration for Qwen2.5-VL-7B-Instruct-AWQ
# Resource limits are already optimized for 7B model in base
