apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: nemotron-70b-dev

namespace: rag-agent

resources:
  - namespace.yaml
  - ../../base

labels:
- includeSelectors: false
  pairs:
    environment: dev

# Dev-specific configuration for Nemotron-3-70B-Instruct
# Resource limits are already optimized for 70B model in base
