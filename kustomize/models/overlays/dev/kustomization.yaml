apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: nemotron-super-49b-dev

namespace: rag-agent

resources:
  - namespace.yaml
  - ../../base

labels:
- includeSelectors: false
  pairs:
    environment: dev

# Dev-specific configuration for Nemotron-Super-49B
# Resource limits are already optimized for 49B FP8 model in base
