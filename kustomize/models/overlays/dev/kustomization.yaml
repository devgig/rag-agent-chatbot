apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: llm-namespace-dev

# No global namespace override — base resources specify their own namespace
# (llm for the namespace + ACR pull secret).

resources:
  - ../../base

labels:
- includeSelectors: false
  pairs:
    environment: dev
