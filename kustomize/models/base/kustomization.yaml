apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: gpt-oss-120b

namespace: multi-agent-dev

resources:
  - hf-external-secret.yaml
  - kaito-workspace.yaml
  - kaito-service.yaml

labels:
- includeSelectors: true
  pairs:
    app: gpt-oss-120b
    component: model-inference
    managed-by: kaito
