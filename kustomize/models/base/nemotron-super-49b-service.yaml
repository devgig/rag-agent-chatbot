# Service for GPU Model Inference
# ================================
# ClusterIP service exposing vLLM OpenAI-compatible API
# Backend connects via: nemotron-super-49b.rag-agent.svc.cluster.local:8000

apiVersion: v1
kind: Service
metadata:
  name: nemotron-super-49b
  namespace: rag-agent
  labels:
    app: nemotron-super-49b
    managed-by: kustomize
spec:
  selector:
    workspace: nemotron-super-49b
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
