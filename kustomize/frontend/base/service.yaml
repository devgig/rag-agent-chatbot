apiVersion: v1
kind: Service
metadata:
  name: rag-agent-frontend
  labels:
    app: rag-agent-frontend
  annotations:
    service.cilium.io/global: "true"
spec:
  selector:
    app: rag-agent-frontend
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  type: ClusterIP
