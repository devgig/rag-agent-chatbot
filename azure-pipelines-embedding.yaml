trigger:
  branches:
    include:
      - main
  paths:
    include:
      - assets/embedding/**
      - kustomize/models/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - assets/embedding/**
      - kustomize/models/**

resources:
- repo: self

variables:
  system.debug: false
  imageRepository: 'rag-agent-chatbot-embedding'
  containerRegistry: 'acr-bytecourier'
  containerRegistryUrl: 'bytecourier.azurecr.io'
  kubernetesServiceConnection: 'k3s-cluster'
  environment: 'K3s'
  dockerfilePath: './assets/embedding/Dockerfile'
  manifestPath: 'kustomize/models/base'
  tag: '$(Build.SourceVersion)'
  poolName: '$(buildAgentPool)'
  base_path: '$(Build.SourceBranchName)'
  source_branch_name: '$(Build.SourceBranchName)'
  imagePullSecret: 'acr-secret'
  namespace: 'rag-agent-dev'

stages:
- stage: BuildAndTest
  displayName: Build and Test Embedding Service
  pool:
    name: $(poolName)
  jobs:
  - job: Build
    displayName: Validate embedding service
    steps:
      - task: Bash@3
        displayName: Validate Python syntax
        inputs:
          targetType: 'inline'
          script: |
            python3 -c "import py_compile; py_compile.compile('assets/embedding/server.py', doraise=True)"

      - task: Bash@3
        displayName: Validate Dockerfile
        inputs:
          targetType: 'inline'
          script: |
            test -f assets/embedding/Dockerfile && echo "Dockerfile exists" || exit 1

- stage: CreateImageAndPublish
  displayName: Create image and Publish
  dependsOn: BuildAndTest
  condition: and(succeeded(), in(variables['Build.Reason'], 'BatchedCI', 'IndividualCI', 'Manual'), eq(variables['Build.SourceBranch'], 'refs/heads/main'))
  pool:
    name: $(poolName)
  jobs:
  - job: BuildAndPush
    displayName: Build image and Push to ACR
    steps:
      - checkout: self

      - task: Docker@2
        displayName: Build and push an image to ACR
        inputs:
          containerRegistry: '$(containerRegistry)'
          repository: '$(imageRepository)'
          command: buildAndPush
          dockerfile: $(dockerfilePath)
          buildContext: '$(Build.SourcesDirectory)/assets/embedding'
          tags: |
            $(tag)
            latest

  - job: PublishManifest
    displayName: Publish manifests
    dependsOn: BuildAndPush
    steps:
      - task: Bash@3
        displayName: Update image in manifests
        inputs:
          targetType: 'inline'
          script: |
            cd kustomize/models/base
            cat <<EOF >qwen3-embedding-image-patch.yaml
            - op: replace
              path: /spec/template/spec/containers/0/image
              value: bytecourier.azurecr.io/$(imageRepository):$(tag)
            EOF

      - task: Bash@3
        displayName: Generate manifests for main branch
        inputs:
          targetType: 'inline'
          script: |
            mkdir manifests
            if [ -d "./kustomize/models/overlays/dev" ]; then
              kubectl kustomize ./kustomize/models/overlays/dev > ./manifests/$(imageRepository)-dev-workloads.yaml
            else
              echo "dev overlay doesn't exist; skipping"
            fi

            if [ -d "./kustomize/models/overlays/prod" ]; then
              kubectl kustomize ./kustomize/models/overlays/prod > ./manifests/$(imageRepository)-prod-workloads.yaml
            else
              echo "prod overlay doesn't exist; skipping"
            fi

      - publish: 'manifests'
        artifact: manifests
