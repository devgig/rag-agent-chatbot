apiVersion: backstage.io/v1alpha1
kind: System
metadata:
  name: rag-agent-chatbot
  description: Multi-agent chatbot system with RAG, vision, and code capabilities for DGX Spark
  annotations:
    dev.azure.com/project-repo: bytecourier/Applications/rag-agent-chatbot
    backstage.io/techdocs-ref: dir:.
  tags:
    - llm
    - rag-agent
    - rag
    - chatbot
    - fastapi
    - react
spec:
  owner: group:default/bytes
  domain: ai-applications

---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: rag-agent-chatbot-backend
  description: FastAPI backend with LangGraph agent orchestration and MCP tool integration
  annotations:
    dev.azure.com/project-repo: bytecourier/Applications/rag-agent-chatbot
    dev.azure.com/build-definition: azure-pipelines-backend.yaml
  tags:
    - python
    - fastapi
    - langgraph
    - mcp
spec:
  type: service
  lifecycle: experimental
  owner: group:default/bytes
  system: rag-agent-chatbot
  providesApis:
    - rag-agent-chatbot-api
  dependsOn:
    - resource:default/milvus
    - resource:default/postgresql

---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: rag-agent-chatbot-frontend
  description: React web UI with Vite and Tailwind for interacting with the rag-agent system
  annotations:
    dev.azure.com/project-repo: bytecourier/Applications/rag-agent-chatbot
    dev.azure.com/build-definition: azure-pipelines-frontend.yaml
  tags:
    - react
    - vite
    - typescript
    - tailwind
spec:
  type: website
  lifecycle: experimental
  owner: group:default/bytes
  system: rag-agent-chatbot
  consumesApis:
    - rag-agent-chatbot-api

---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: rag-agent-chatbot-api
  description: REST and WebSocket API for rag-agent chatbot interactions
  tags:
    - rest
    - websocket
spec:
  type: openapi
  lifecycle: experimental
  owner: group:default/bytes
  system: rag-agent-chatbot
  definition: |
    openapi: 3.0.0
    info:
      title: RAG Agent Chatbot API
      version: 0.1.0
    paths:
      /ingest:
        post:
          summary: Upload documents for RAG ingestion
      /ingest/status/{task_id}:
        get:
          summary: Check document ingestion status
      /ws:
        get:
          summary: WebSocket endpoint for chat interactions
